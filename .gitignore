# accounts + progress = private data, never commit
users.json
users.json.tmp
progress.json

# scratch file the terminal game writes your answers into
work.py

__pycache__/
*.pyc

# AI key stays on this machine only
claude_key.txt

# installed agent skills, not part of the project
.agents/

# scratch file the File handling topic writes
note.txt
data.txt

# installed agent skills, not part of the project
.claude/
skills-lock.json

# AI key stays on this machine only
ai_key.txt
